<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>
<!-- xml 이름상관 없음. *.xml로 설정했으니까 -->
<!-- 이 xml이 어떤 interface와 연결되는지 설정 -->
<mapper namespace="com.gong.gu.dao.MemberDAO">

	<select id ="overlay" resultType = "String"> <!-- 메서드로서의 오버레이  -->
		SELECT user_id FROM user_info WHERE user_id = #{param1}
	</select>
	
	<insert id = "join" parameterType="hashmap">
	INSERT INTO user_info 
		(user_id, user_pw, user_name, user_gender, user_birth, user_email, user_phone,
		user_address, user_admin)
	VALUES
		(#{user_id}, #{user_pw}, #{user_name}, #{user_gender}, #{user_birth}, #{user_email}, 
		#{user_phone}, #{user_address}, #{user_admin})
		<!-- 인서트문 안에는 name을 넣는다.  -->
	</insert>
	
	<select id = "login" resultType="int">
		SELECT COUNT(user_id) FROM user_info
				WHERE user_id = #{param1} AND user_pw = #{param2}
	</select>
	
	<!--  아이디 찾기 parameterType="String" 뺐음-->
	<select id="idfind" resultType="String" parameterType="String">
		SELECT user_id FROM user_info WHERE user_name = #{param3} AND user_birth = #{param5}
		AND user_email = #{param6}
	</select> <!--위 구문 수정이 필요하다 -->
	
	<!--  <select id="idfind" parameterType="hashmap">
		SELECT COUNT(user_id) FROM user_info 
			WHERE user_id = #{param1}
		AND user_name = #{param3} 
		AND user_birth = #{param5}
		AND user_email = #{param6}
	</select> 
	--> 


</mapper>